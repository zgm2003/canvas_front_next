# 无限画布前端

Consumer frontend for backend platform `infinite_canvas`.

## Commands

```powershell
npm install
npm run dev
npm run verify
```

The default development port is `3000`. Use an explicit alternate port when it is occupied:

```powershell
npm run dev -- --port 3001
```

Architecture and integration boundaries are documented in `docs/frontend-architecture.md`.
